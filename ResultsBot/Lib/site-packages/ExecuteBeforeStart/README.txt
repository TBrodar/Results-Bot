Python code files (.py) from this directory will be executed before code selected in ResultsBot application, on Start command.

Mouse, App, Screen, Keyboard functions are implemented in C# and are not avaliable like builtin functions to all scopes.
Scope od imported modules doesn't contain them, so this is the way you can "import" classes/functions which use them.